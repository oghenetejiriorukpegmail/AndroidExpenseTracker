<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Define path for temporary camera images in app's cache directory -->
    <cache-path name="camera_images" path="images/"/>
    <!-- You could also use files-path for internal storage, or external-path/external-cache-path if needed -->
</paths>